import 'package:flutter/material.dart';

class PetunjukAdmin extends StatelessWidget {
  const PetunjukAdmin({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Petunjuk Admin')),
    );
  }
}
